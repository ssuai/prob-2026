A discrete random variable X has the following probability mass function (PMF):
p_X(x) = 0.2{if } x = -2, 0.5{if } x = 1, 0.3{if } x = 4, 0{otherwise}

1. Calculate the expectation E[X].
2. Calculate the variance var(X) using the formula var(X) = E[X^2] - (E[X])^2.