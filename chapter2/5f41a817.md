# 1.


1.
A fair coin is tossed twice. Let (X) be the number of heads, and let (Y) be defined as (Y=1) if the first toss is a head, and (Y=0) otherwise. Find the joint probability mass function (p_{X,Y}(x,y)), then use it to derive the marginal probability mass function (p_X(x)). Finally, compute the conditional probability (P(X=1 \mid Y=1)) and briefly interpret its meaning.
