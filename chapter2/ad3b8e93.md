### Problem 3 - Hard

In a matching experiment, $n$ distinct items are randomly assigned to $n$ unique slots, with exactly one item per slot. Each possible assignment is equally likely. Let $X$ be the random variable representing the total number of items that are assigned to their correct corresponding slots.Calculate the variance $var(X)$.