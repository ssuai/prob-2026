# Intermediate Problem

A discrete random variable X has PMF:

p_X(x) = {
  1/10 if x = -2,
  2/10 if x = -1,
  4/10 if x = 0,
  2/10 if x = 1,
  1/10 if x = 2
}

Let Y = X^2.

Find the PMF of Y and compute E[Y] and var(Y).
