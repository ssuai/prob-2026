 Easy Problem — Cache Hit Rate Estimation
Problem: Cache Hit Rate in a Web Server System
You are operating a web server with a caching system. For each request, define a random variable XiX_i
Xi​ representing whether the request results in a cache hit:

Xi={1cache hit0cache missX_i = \begin{cases} 1 & \text{cache hit} \\ 0 & \text{cache miss} \end{cases}Xi​={10​cache hitcache miss​
The cache hit probability for each request is known to be p=0.8p = 0.8
p=0.8, so E[Xi]=0.8E[X_i] = 0.8
E[Xi​]=0.8 and Var(Xi)=p(1−p)=0.16\text{Var}(X_i) = p(1-p) = 0.16
Var(Xi​)=p(1−p)=0.16. Requests are i.i.d. The empirical hit rate over nn
n requests is defined as

Mn=X1+X2+⋯+Xnn.M_n = \frac{X_1 + X_2 + \cdots + X_n}{n}.Mn​=nX1​+X2​+⋯+Xn​​.
(a) [Markov Inequality]Suppose the response time TiT_i
Ti​ for a single request is always non-negative with mean E[Ti]=50E[T_i] = 50
E[Ti​]=50 ms. Using the Markov inequality, find an upper bound on P(Ti≥250 ms)P(T_i \geq 250 \text{ ms})
P(Ti​≥250 ms).
(b) [Chebyshev Inequality]
Apply the Chebyshev inequality to XiX_i
Xi​ to find an upper bound on P(∣Xi−0.8∣≥0.5)P(|X_i - 0.8| \geq 0.5)
P(∣Xi​−0.8∣≥0.5). Since Xi∈{0,1}X_i \in \{0, 1\}
Xi​∈{0,1}, also compute the exact probability and compare it with the bound.
(c) [Weak Law of Large Numbers]Following the proof technique from slide 5-1, p.16, express the upper bound on P(∣Mn−p∣≥0.05)P(|M_n - p| \geq 0.05)
P(∣Mn​−p∣≥0.05) as a function of nn
n. Then determine the minimum number of requests nn
n needed to make this probability at most 0.010.01
0.01. 