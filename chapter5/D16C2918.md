Problem 3 — Hard: Comparing Chebyshev, Hoeffding, and CLT Tail Bounds
Topic: Concentration inequalities vs. CLT; rigorous reasoning about rates of convergence
Problem Statement:
Let X1,X2,…,XnX_1, X_2, \ldots, X_n
X1​,X2​,…,Xn​ be i.i.d. random variables taking values in [0,1][0, 1]
[0,1], with unknown mean μ=E[X1]\mu = E[X_1]
μ=E[X1​] and variance σ2=Var(X1)≤14\sigma^2 = \text{Var}(X_1) \leq \tfrac{1}{4}
σ2=Var(X1​)≤41​. Let Xˉn\bar{X}_n
Xˉn​ denote the sample mean, and fix ε>0\varepsilon > 0
ε>0.
Three different upper bounds on the tail probability P(Xˉn−μ≥ε)P(\bar{X}_n - \mu \geq \varepsilon)
P(Xˉn​−μ≥ε) are available:

Chebyshev: P(Xˉn−μ≥ε)≤σ2nε2P(\bar{X}_n - \mu \geq \varepsilon) \leq \dfrac{\sigma^2}{n\varepsilon^2}
P(Xˉn​−μ≥ε)≤nε2σ2​
Hoeffding: P(Xˉn−μ≥ε)≤exp⁡(−2nε2)P(\bar{X}_n - \mu \geq \varepsilon) \leq \exp(-2n\varepsilon^2)
P(Xˉn​−μ≥ε)≤exp(−2nε2)
CLT (approximate, large nn
n): P(Xˉn−μ≥ε)≈1−Φ ⁣(εnσ)P(\bar{X}_n - \mu \geq \varepsilon) \approx 1 - \Phi\!\left(\dfrac{\varepsilon \sqrt{n}}{\sigma}\right)
P(Xˉn​−μ≥ε)≈1−Φ(σεn​​)

(a) For σ2=14\sigma^2 = \tfrac{1}{4}
σ2=41​, ε=0.05\varepsilon = 0.05
ε=0.05, and n=1000n = 1000
n=1000, compute all three bounds numerically and rank them from tightest to loosest. Comment briefly on which bound is most informative in this regime.
(b) Prove that for any fixed ε>0\varepsilon > 0
ε>0 and σ2>0\sigma^2 > 0
σ2>0, the Hoeffding bound decays faster than the Chebyshev bound as n→∞n \to \infty
n→∞. Specifically, show that

lim⁡n→∞exp⁡(−2nε2)σ2/(nε2)=0.\lim_{n \to \infty} \frac{\exp(-2n\varepsilon^2)}{\sigma^2 / (n\varepsilon^2)} = 0.n→∞lim​σ2/(nε2)exp(−2nε2)​=0.
(c) The CLT gives an *approximation*, not an upper bound. Using the standard tail estimate

1−Φ(x)≤1x2πe−x2/2for x>0,1 - \Phi(x) \leq \frac{1}{x\sqrt{2\pi}} e^{-x^2/2} \quad \text{for } x > 0,1−Φ(x)≤x2π​1​e−x2/2for x>0,
derive an upper bound on the CLT-based tail probability and compare its exponential decay rate (in nn
n) to that of the Hoeffding bound. Under what condition on σ2\sigma^2
σ2 is the CLT-derived bound asymptotically tighter than Hoeffding's?
(d) Despite (b) and (c), explain — using your numerical results from (a) and the structural form of each inequality — why a practitioner working with a specific finite sample might still prefer Chebyshev's inequality in certain situations. Identify at least one such situation.
Assumptions / Given Information:

The XiX_i
Xi​ are i.i.d. with support contained in [0,1][0,1]
[0,1], justifying Hoeffding's inequality.
The variance bound σ2≤14\sigma^2 \leq \tfrac{1}{4}
σ2≤41​ holds for any random variable supported on [0,1][0,1]
[0,1].
For part (c), you may assume nn
n is large enough that the CLT approximation is valid in the relevant sense; the question concerns the asymptotic decay rate of the *Gaussian tail*, not the approximation error itself.