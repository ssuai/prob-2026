# Problem 3 (Hard)

When a `VArray` reaches its maximum capacity, a "resize" operation is triggered to allocate a new array and copy the existing data. The execution time of this resize operation, $Z$, is strictly non-negative ($Z \ge 0$). It is known that the mean execution time is $1000\mu s$ and the variance is $10000(\mu s)^2$.

To prepare for the worst-case scenario, you need to find the maximum possible probability that the resize operation takes $1500\mu s$ or more, i.e., $P(Z \ge 1500)$. Calculate this upper bound using both of the following methods, and briefly state which inequality provides a tighter (more useful) bound:
(1) Markov's Inequality
(2) Chebyshev's Inequality