2.
An independent and identically distributed (i.i.d.) sample of size n = 100 is drawn from an infinite population with an unknown mean \mu and a variance \sigma^2 = 4. The sample mean is defined as:

Using the Chebyshev inequality, derive the exact upper bound for the probability that the absolute difference between the sample mean M_n and the true population mean \mu is greater than or equal to 0.5. That is, calculate the upper bound of P(|M_n - \mu| \ge 0.5)