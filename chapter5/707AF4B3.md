Problem 2 — Intermediate: Estimating a Proportion with CLT and a Variance Bound
Topic: Central Limit Theorem combined with a variance inequality (worst-case Bernoulli variance)
Problem Statement:
A pollster wants to estimate the unknown proportion p∈(0,1)p \in (0, 1)
p∈(0,1) of voters in a large population who support a particular candidate. She draws an i.i.d. sample X1,…,XnX_1, \ldots, X_n
X1​,…,Xn​ where each Xi∼Bernoulli(p)X_i \sim \text{Bernoulli}(p)
Xi​∼Bernoulli(p), and uses the sample proportion p^n=1n∑i=1nXi\hat{p}_n = \frac{1}{n}\sum_{i=1}^n X_i
p^​n​=n1​∑i=1n​Xi​ as her estimator.
She wants to choose nn
n large enough so that, *regardless of the true value of* pp
p,

P(∣p^n−p∣≤0.02)≥0.95.P\left(|\hat{p}_n - p| \leq 0.02\right) \geq 0.95.P(∣p^​n​−p∣≤0.02)≥0.95.
(a) Using the Central Limit Theorem, write down an approximate expression for P(∣p^n−p∣≤0.02)P(|\hat{p}_n - p| \leq 0.02)
P(∣p^​n​−p∣≤0.02) in terms of nn
n, pp
p, and the standard normal CDF Φ\Phi
Φ.
(b) Show that p(1−p)≤14p(1-p) \leq \tfrac{1}{4}
p(1−p)≤41​ for all p∈[0,1]p \in [0,1]
p∈[0,1], and use this inequality to find the smallest sample size nn
n that guarantees the coverage requirement above for *every* possible value of pp
p.

Assumptions / Given Information:

The sample is i.i.d., and nn
n is large enough for the CLT approximation to be reasonable.
Use Φ−1(0.975)≈1.96\Phi^{-1}(0.975) \approx 1.96
Φ−1(0.975)≈1.96.
The population is large enough that sampling can be treated as with replacement.