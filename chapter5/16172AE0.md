🟡 Medium Problem — SGD Mini-Batch Gradient Estimation
Problem: Gradient Accuracy in Mini-Batch Training
You are training a deep learning model using mini-batch SGD. Let μ\mu
μ denote the true gradient of the loss function (along a particular parameter direction) averaged over the entire dataset. Each training sample produces a gradient GiG_i
Gi​, and these are i.i.d. with

E[Gi]=μ=0.5,Var(Gi)=σ2=4.E[G_i] = \mu = 0.5, \quad \text{Var}(G_i) = \sigma^2 = 4.E[Gi​]=μ=0.5,Var(Gi​)=σ2=4.
The SGD estimate from a mini-batch of size nn
n is

G^n=1n∑i=1nGi.\hat{G}_n = \frac{1}{n}\sum_{i=1}^n G_i.G^n​=n1​i=1∑n​Gi​.
(a) [WLLN — Batch Size Design]For stable training, the mini-batch gradient G^n\hat{G}_n
G^n​ should lie within ±0.1\pm 0.1
±0.1 of the true gradient μ=0.5\mu = 0.5
μ=0.5 with probability at least 95%. Using the Chebyshev-based WLLN bound from slide 5-1, p.16, find the minimum batch size nn
n satisfying this requirement. Justify why the independence and identical distribution assumptions are needed.
(b) [CLT — Sharper Estimate]The team decides to use n=400n = 400
n=400. Using the CLT, approximate P(∣G^n−0.5∣≥0.1)P(|\hat{G}_n - 0.5| \geq 0.1)
P(∣G^n​−0.5∣≥0.1). Standardize using Zn=(G^n−μ)/(σ/n)Z_n = (\hat{G}_n - \mu)/(\sigma/\sqrt{n})
Zn​=(G^n​−μ)/(σ/n​) and use Φ(1)≈0.8413\Phi(1) \approx 0.8413
Φ(1)≈0.8413.
**(c) [Comparison & Practical Insight]**
For the same n=400n = 400
n=400, compute the Chebyshev bound on P(∣G^n−0.5∣≥0.1)P(|\hat{G}_n - 0.5| \geq 0.1)
P(∣G^n​−0.5∣≥0.1). Compare it with your CLT answer in (b). Explain, in 2–3 sentences, why the CLT gives a much sharper estimate, and justify the common ML intuition *"larger batch size → more stable gradients"* using both WLLN and CLT perspectives.
