Level: Hard (Focus: Chapter 3 - Conditional Variance and Estimation)Problem:In a signal processing scenario, we want to estimate a signal $X$ based on a noisy observation $Y$. We are given that the conditional expectation of $X$ given $Y$ is a linear estimator: $E[X|Y] = 0.8Y$. We also know that the conditional variance is $Var(X|Y) = 2$.The observation $Y$ itself is a random variable with $E[Y] = 0$ and $Var(Y) = 10$.Calculate the unconditional mean $E[X]$ using the Law of Iterated Expectations.Calculate the unconditional variance $Var(X)$ using the Law of Total Variance (Conditional Variance Formula).If we use $E[X|Y]$ as our estimator for $X$, what is the Mean Squared Error (MSE) of this estimation? (Hint: Think about what $Var(X|Y)$ represents in the context of estimation error.)