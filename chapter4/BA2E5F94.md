Problem 2 — Intermediate: Covariance and Conditional Expectation in a Hierarchical Model
Topic emphasis: Conditional expectation combined with covariance (two concepts, multi-step reasoning)
Problem statement:
A factory produces electronic components in batches. The number of defective components NN
N in a randomly chosen batch follows a Poisson distribution with random rate Λ\Lambda
Λ, where Λ\Lambda
Λ itself is a random variable distributed as Λ∼Gamma(α,β)\Lambda \sim \text{Gamma}(\alpha, \beta)
Λ∼Gamma(α,β) (shape α\alpha
α, rate β\beta
β). That is,
N∣Λ=λ  ∼  Poisson(λ),Λ∼Gamma(α,β).N \mid \Lambda = \lambda \;\sim\; \text{Poisson}(\lambda), \qquad \Lambda \sim \text{Gamma}(\alpha, \beta).N∣Λ=λ∼Poisson(λ),Λ∼Gamma(α,β).
(a) Using the tower property (law of total expectation), compute E[N]\mathbb{E}[N]
E[N] and E[N2]\mathbb{E}[N^2]
E[N2]. From these, derive Var(N)\text{Var}(N)
Var(N) and verify whether NN
N is overdispersed relative to a standard Poisson random variable (i.e., whether Var(N)>E[N]\text{Var}(N) > \mathbb{E}[N]
Var(N)>E[N]).
(b) Compute Cov(N,Λ)\text{Cov}(N, \Lambda)
Cov(N,Λ). Then explain in one or two sentences the intuitive meaning of the sign and magnitude of this covariance in the context of the production process.
(c) Without computing the full marginal distribution of NN
N, argue why NN
N and Λ\Lambda
Λ cannot be independent.
Assumptions / Given information:

The Gamma distribution uses the rate parameterization: E[Λ]=α/β\mathbb{E}[\Lambda] = \alpha/\beta
E[Λ]=α/β and Var(Λ)=α/β2\text{Var}(\Lambda) = \alpha/\beta^2
Var(Λ)=α/β2.
α>0\alpha > 0
α>0 and β>0\beta > 0
β>0.
Standard identity: for N∣Λ∼Poisson(Λ)N \mid \Lambda \sim \text{Poisson}(\Lambda)
N∣Λ∼Poisson(Λ), E[N∣Λ]=Var(N∣Λ)=Λ\mathbb{E}[N \mid \Lambda] = \text{Var}(N \mid \Lambda) = \Lambda
E[N∣Λ]=Var(N∣Λ)=Λ.
Students should use the laws of total expectation and total variance rather than computing the marginal PMF of NN
N directly.