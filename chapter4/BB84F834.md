Hard Problem

A coin has an unknown probability of heads Y, where

Y∼Uniform(0,1).

Given Y=y, the coin is tossed n times independently, and X denotes the number of heads.

1. Find the conditional expectation E[X∣Y].
2. Use the law of total expectation to compute E[X].
3. Find the conditional variance Var(X∣Y).
4. Use the law of total variance,
	Var(X)=E[Var(X∣Y)]+Var(E[X∣Y]),
   to compute Var(X).