Easy Problem

Let X be a discrete random variable with the following PMF:

pX(x) = 0.4, x=1,
	  0.6, x=3,
	  0, otherwise.

1. Find the moment generating function Mx(s).
2. Use the MGF to compute E[X].