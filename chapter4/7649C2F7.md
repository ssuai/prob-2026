Problem statement:
Let XX
X be a continuous random variable uniformly distributed on the interval (0,1)(0, 1)
(0,1). Define a new random variable
Y=−1λln⁡(1−X),Y = -\frac{1}{\lambda}\ln(1 - X),Y=−λ1​ln(1−X),
where λ>0\lambda > 0
λ>0 is a fixed constant.
(a) Derive the probability density function (PDF) fY(y)f_Y(y)
fY​(y) of YY
Y using the CDF method, and explicitly state the support of YY
Y.
(b) Identify the named distribution of YY
Y and explain briefly why this transformation is useful for simulating samples from that distribution given only a uniform random number generator.
Assumptions / Given information:

X∼Uniform(0,1)X \sim \text{Uniform}(0,1)
X∼Uniform(0,1), so fX(x)=1f_X(x) = 1
fX​(x)=1 for x∈(0,1)x \in (0,1)
x∈(0,1) and 00
0 otherwise.
λ\lambda
λ is a strictly positive real constant.
The natural logarithm ln⁡(⋅)\ln(\cdot)
ln(⋅) is used.
Students are expected to justify monotonicity of the transformation before applying the change-of-variable technique.