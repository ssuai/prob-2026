# Hard Problem

Let X follow a normal distribution N(mu, sigma^2).

1. Derive the moment generating function M_X(s).
2. Use the MGF to compute E[X] and Var(X).
3. Let Y = 2X + 3. Find the distribution of Y using the MGF approach.
