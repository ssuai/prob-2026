Problem 3 — Hard: MGF-Based Proof Involving a Derived Distribution
Topic emphasis: Moment generating functions, derived distributions, and a non-trivial proof (deeper reasoning)
Problem statement:
Let X1,X2,…,XnX_1, X_2, \dots, X_n
X1​,X2​,…,Xn​ be independent and identically distributed random variables, each with moment generating function MX(t)M_X(t)
MX​(t) defined and finite on an open interval containing zero. Let NN
N be a random variable independent of the XiX_i
Xi​'s, taking values in {0,1,2,… }\{0, 1, 2, \dots\}
{0,1,2,…}, with MGF MN(t)M_N(t)
MN​(t) also finite in a neighborhood of zero. Define the random sum
SN=∑i=1NXi,with the convention S0=0.S_N = \sum_{i=1}^{N} X_i, \qquad \text{with the convention } S_0 = 0.SN​=i=1∑N​Xi​,with the convention S0​=0.
(a) Prove from first principles, using the law of total expectation conditioned on NN
N, that the MGF of SNS_N
SN​ satisfies
MSN(t)=MN ⁣(ln⁡MX(t)),M_{S_N}(t) = M_N\!\big(\ln M_X(t)\big),MSN​​(t)=MN​(lnMX​(t)),
for all tt
t in a neighborhood of zero where both sides are well-defined. Be careful to justify the interchange of expectation and summation, and clearly state where independence is used.
(b) Suppose now that N∼Geometric(p)N \sim \text{Geometric}(p)
N∼Geometric(p) with PMF P(N=k)=(1−p)kpP(N = k) = (1-p)^k p
P(N=k)=(1−p)kp for k=0,1,2,…k = 0, 1, 2, \dots
k=0,1,2,…, and each Xi∼Exponential(μ)X_i \sim \text{Exponential}(\mu)
Xi​∼Exponential(μ) with rate μ>0\mu > 0
μ>0, and assume XiX_i
Xi​ and NN
N are mutually independent. Use the result from part (a) to derive the MGF of SNS_N
SN​ in closed form. Then identify the distribution of SNS_N
SN​, paying special attention to the mixed nature of its distribution (i.e., the fact that SNS_N
SN​ has both a discrete atom and a continuous component). Specify precisely:

the probability mass at SN=0S_N = 0
SN​=0,
the conditional distribution of SNS_N
SN​ given SN>0S_N > 0
SN​>0,
and how these combine to form the full distribution of SNS_N
SN​.

(c) Briefly explain why this random sum construction generalizes the notion of a "compound distribution," and give one real-world example (e.g., insurance, queueing, reliability) where the MGF identity in part (a) provides practical analytical leverage.
Assumptions / Given information:

All MGFs are assumed to exist on an open interval containing 00
0.
XiX_i
Xi​'s are i.i.d. and independent of NN
N.
The convention S0=0S_0 = 0
S0​=0 is used so that SNS_N
SN​ is well-defined when N=0N = 0
N=0.
In part (b), the geometric distribution starts at 00
0 (number of failures before the first success).
For the Exponential(μ)(\mu)
(μ) distribution: MX(t)=μ/(μ−t)M_X(t) = \mu/(\mu - t)
MX​(t)=μ/(μ−t) for t<μt < \mu
t<μ.
Students are expected to interpret the resulting mixed distribution rigorously, not just write down an MGF.