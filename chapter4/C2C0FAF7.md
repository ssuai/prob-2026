### Q3 - Hard
MGF and Conditional Distribution: Poisson
Let X ~ Poisson(λ₁) and Y ~ Poisson(λ₂) be independent. (a) Use the MGF to find the distribution of Z = X + Y. (b) Given Z = n, find the conditional distribution of X.
