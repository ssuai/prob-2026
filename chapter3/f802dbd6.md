# Problem 1 (Easy)

Let X be a continuous random variable with the probability density function (PDF) given by:
f_X(x) = cx   (if 0 <= x <= 2)
f_X(x) = 0    (otherwise)

(a) Find the value of the constant c that makes f_X(x) a valid PDF.
(b) Calculate the probability P(0 <= X <= 1).