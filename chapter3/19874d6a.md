 Problem 3. (Hard) Joint Density and Conditional ProbabilityTopic: Bivariate distributions and Marginalization.Consider two continuous random variables $X$ and $Y$ with the following joint PDF:$$f_{X,Y}(x, y) = \begin{cases} cxy, & \text{if } 0 \le x \le 2 \text{ and } 0 \le y \le 1 \\ 0, & \text{otherwise} \end{cases}$$Solve for the constant $c$ such that the joint PDF satisfies the normalization property.Determine the marginal PDF of $Y$, $f_Y(y)$, by integrating out the variable $X$.Calculate the conditional PDF $f_{X|Y}(x | y)$ for $Y = 0.5$.